import { Button } from "@/components/button";

const name = "button";

const components = ["button"];

const Component = () => {
  return <Button>You can click me</Button>;
};

export const ButtonDemo = {
  Component,
  components,
  name,
};
