import { Switch } from "@/components/switch";

const name = "switch";

const components = ["switch"];

const Component = () => {
  return <Switch />;
};

export const SwitchDemo = {
  Component,
  components,
  name,
};
